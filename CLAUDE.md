# Local Preview Board — 작업 안내

김유정 개인용 퍼블릭 도구. 회사/프로젝트와 무관 — 일반 원칙은
`~/.claude/CLAUDE.md` 를 따른다.

- 단일 파일(`local-preview-board.html`)이라 클래스 재사용 범위가 좁지만,
  `.chip`/`.row`/`.fw` 같은 공통 클래스를 고칠 땐 그 클래스를 쓰는 다른
  요소(사이드바 row, 프리뷰 카드 등)에도 같이 적용되는 게 맞는지 확인할 것.
- `file://` 상대경로 제약(README 참고) 때문에 이 파일은 항상 검수 대상
  폴더 안에 물리적으로 복사해서 써야 한다 — 이 구조 자체를 "고치려는"
  시도(예: fetch로 pages.json 읽기)는 브라우저 보안 제약에 막힌다는 걸
  이미 확인했으니 재시도하지 말 것.
- 변경 후에는 README 사용법 설명이 실제 동작과 맞는지 같이 확인한다.
